{
  "name": "station-two",
  "version": "1.1.0",
  "description": "We're taking trivia to the next level.",
  "private": true,
  "author": "Sam Sverko",
  "license": "MIT",
  "main": "server.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SamSverko/station-two.git"
  },
  "bugs": {
    "url": "https://github.com/SamSverko/station-two/issues"
  },
  "homepage": "https://github.com/SamSverko/station-two#readme",
  "scripts": {
    "dev-restart": "pm2 restart ecosystem.config.js --only station-two-dev",
    "dev-start": "pm2 start ecosystem.config.js --only station-two-dev --watch",
    "dev-stop": "pm2 stop ecosystem.config.js --only station-two-dev",
    "restart": "pm2 restart ecosystem.config.js --only station-two",
    "start": "pm2 start ecosystem.config.js --only station-two",
    "stop": "pm2 stop ecosystem.config.js --only station-two",
    "test": "standard"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-mongo-sanitize": "^2.0.0",
    "express-validator": "^6.4.0",
    "helmet": "^3.22.0",
    "mongodb": "^3.5.6",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "standard": "^14.3.3"
  }
}
